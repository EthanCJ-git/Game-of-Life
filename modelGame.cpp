#include "modelGame.h"


ModelGame::ModelGame(const int width, const int height)
{
	Board = new vector<vector<int>>(height);
	for(int i = 0; i < height; i++)
	{
		vector<int> temp = new vector<int>(width);
		temp.fill
		Board[i] = temp;
	}//populate the Board vector
}

ModelGame::check(int x, int y)
{
	int sum = 0;
	sum += 
}
